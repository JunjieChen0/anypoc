Path to source code: /opt/firefox-js

Path to built binary: /opt/firefox-js/obj-js-asan/dist/bin/js

The `js` binary must be run with the `--fuzzing-safe` to make sure we are not using unreasonable internal APIs.
