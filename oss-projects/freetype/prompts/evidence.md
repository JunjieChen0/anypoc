If the PoC triggers the bug by passing intentionally invalid parameters to FreeType API functions (e.g., NULL pointers, negative sizes, calling functions on uninitialized or already-freed objects) or by calling APIs in sequences that no real application would use, it is not an acceptable PoC. The bug must be triggered by the content of a crafted font file processed through normal, realistic API usage.
