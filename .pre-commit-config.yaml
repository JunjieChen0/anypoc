repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.9.6
    hooks:
      - id: ruff
        args: [--fix]
        exclude: ^kbscan/
        verbose: true
      - id: ruff-format
        exclude: ^kbscan/
        verbose: true
